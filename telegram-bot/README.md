# Telegram Bot

## Setup

1. Create `.env` based on `.env.example`.
2. Install deps:

```bash
pip install -r requirements.txt
```

3. Run:

```bash
python bot.py
```

## Notes
- `/start` sends a static info message and three buttons.
- `FREE_OPEN_URL` should point to your Mini App deep link that opens a pack.
